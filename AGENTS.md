# RoadMap2U — Agent Onboarding

Context file for AI agents (Codex, Claude, Cursor, etc.) and new contributors. Read this before changing anything. The README covers *what* the app is; this covers *why it is the way it is* and *what will break if you ignore it*.

## What this is

A local-first Angular PWA for **neurodivergent users** where goals grow as a tree. Missed deadlines are never failures — they become **branch points** that sprout alternative paths. Live at `https://toydrum.github.io/RoadMap2U/`, deployed from `main` via GitHub Actions → GitHub Pages.

## Product rules (NON-NEGOTIABLE — these override any technical preference)

1. **Compass language, never action-demands.** Every question in the UI must: (1) locate — "estás aquí"; (2) soothe — "respira, partamos de aquí"; (3) frame the place as fertile; only then show paths, phrased as first-person statements ("Sigo aquí, a mi ritmo"), never commands. "¿Qué quieres hacer?" is banned.
2. **No shame mechanics.** No streaks, no "overdue/atrasado", no red alarm colors, no counters of ignored prompts. A passed date opens a gentle conversation with dignified exits. There is NO "failed" state — the `branched` status is earned through transformation and cannot be hand-picked in the UI.
3. **Nothing is deleted by accident.** User-facing "delete" = archive (`archivedAt`, recoverable). Permanent deletion exists ONLY inside Settings→Archive, requires confirmation, and **auto-downloads a full backup before executing** (`BackupService.download`), then writes sync tombstones (`deletedAt`) — records are never physically removed. **Every archive/dismiss toast offers Undo** (`common.undo`, duration `UNDO_MS` from `toast.service.ts`); action toasts auto-expire unless `sticky: true` (the SW update offer is the only sticky one). Undo actions must **re-read the live record and re-stamp** (`unarchiveMany`/`revive`/`restore` patterns) — never re-save a captured pre-action record; its stale `rev` loses cross-tab LWW.
4. **Predictability.** All decorative randomness is **deterministic** from stable ids via `hash()` (`tree-layout.ts`). A user's tree/forest must NEVER reshuffle between sessions. If you add visual variety, derive it from ids, not `Math.random()`.
5. **Motion is opt-out-able, always.** Every animation must respect `MotionService.reduced()` (combines `prefers-reduced-motion` with the user setting; global `.reduce-motion` CSS class is the backstop). Lightning/rain are photosensitivity-sensitive: flashes are small, slow (≥4s apart), low-contrast, and fully removed under reduced motion.
6. **Everything bilingual.** All copy lives in `core/i18n/es.ts` (source of truth) and `en.ts` (`const EN: Dict` — the compiler enforces parity). Never hardcode user-facing strings in templates; never index dictionaries with dynamic string keys (kills the type guarantee). **Counts pluralize via `i18n.plural(count, dict.section.key)`** where the key is a `{ one, many }` sentence pair — whole-sentence pairs, never word-splicing, so Spanish gender/order works.
7. **Privacy: local-first storage, cloud through ONE seam.** The forest lives and works in this device's IndexedDB — offline always. Accounts/cloud (Cognito + DynamoDB via API Gateway) are being integrated for the families-and-friends model: the backend is SIMULATED today (`core/config.ts` `backend: 'mock'` → the on-device `roadmap2u-mockcloud` DB); flipping that config to `'aws'` IS the migration (**step-by-step runbook: `docs/aws-connect.md`** — identity can connect before the API exists), and **login becomes mandatory at AWS go-live** (owner decision 2026-07-06; `requireAuth` flag + `authRequiredGate` are already wired on every route, inert). Network calls are allowed ONLY inside `core/api/` and `core/auth/` behind the `API_CLIENT`/`AUTH_PROVIDER` seams — never sprinkled through features. **No analytics, no ads, no tracking, ever.** Signing in/out NEVER mutates local forest data, and auth state lives in `meta` keys (`auth.identity`) — never in `Settings`, never in backup exports. `aws-amplify` may be mentioned ONLY in `cognito-auth.provider.ts` and only via dynamic `import()` (it must stay a lazy chunk — after every build, `main-*.js` must NOT match `cognito-idp|amazonaws\.com`). Notifications exist ONLY as the opt-in **whispers** (`core/accompaniment.service.ts`): orientation QUESTIONS ("¿dónde sientes que estás?") that land on the check-in — **they may ask how you are; they may never tell you what to do**. No task names in the QUESTION, no counts of missed anything, silent (no sound/vibration), waking hours only (9–21), never during a session, never within 3h of a check-in, varying interval (fixed pings go blind; rhythm 'surprise' = deterministic pseudo-random 1.5–6h gaps). **Beat two**: ~45s after the question (answered or let go), ONE tiny low-energy offer — the first LEAF in the ranked pool — with a 2-minute start action; one shot, dismissible, never repeated, dropped silently if a session already exists or 10 min passed. Off by default; the toggle is the permission gesture. Technical floor: they live only while the app is open somewhere (no push backend) — never promise more.
8. **Every modal sheet uses `shared/ui/sheet.directive.ts` (`appSheet`)** — it provides Escape-to-close (topmost of the stack only), initial focus (`[autofocus]` else host), a minimal Tab trap, and focus restoration to the opener. New sheets MUST wire `appSheet (sheetClose)="…same close expr…"` plus the usual backdrop click-close.
9. **The check-in gate diverts once per app-open** (`SessionGate` in `app.routes.ts`) and it guards **`/ahora`** (home), not the forest: the first Ahora visit may redirect to the ritual (cooldown respected); later tab taps never do, and the forest NEVER diverts. **The ritual is TWO screens flat** (`feeling → destination`, plus a one-time `welcome` step when `!onboarded`): the optional notita folds into the feeling screen as an expander (never its own screen again), and the destination screen merges the branch shortcuts (the 4 freshest live branches) with the tree ring and "Solo mirar el bosque". A branch tap records the check-in WITH `nodeId` (the 📍 moves inside `CheckinsRepo.record`); ring/forest taps record without one; skip, back and express record exactly what they always did. **Passed-date reviews never interrupt the ritual** — Ahora's 🍂 banner and the tree-view banner are their only homes. Express/skip exits land on `/ahora`; explicit destinations keep their word. The wind rose is the manual re-entry (the 🔍 finder «Buscar en mi bosque» 0.0.68 is the separate UTILITARIAN door: fuzzy accent-insensitive match in features/forest/finder.ts (pure) → /tree/:id?locate= — locate PANS and opens nothing; ?node= is the opening variant. First-visit «¿qué es esto?» chips: shared/ui/hint-chip.ts + Settings.hintsSeen, dismiss-forever, COGA help-in-context) — don't reintroduce per-navigation gating, and don't rename it: the poetic labels are loved; when a flow confuses, restructure the flow. The ring interleaves two radii past 9 trees (deterministic formula in `check-in.ts`; `--ring-count` feeds pill sizing).
10. **Focus sessions live in `core/focus-session.service.ts`**, not in the timer page: cross-route, adopts the open IndexedDB session row on reload, one-time 🌸 cue at planned time (toast + tab-title suffix), never auto-ends, never alarms. The timer page is a thin view over it.
11. **Ahora is home + the ONE-suggestion doctrine.** `''` and `**` redirect to `/ahora` — the companion surface: the thread ("Ibas aquí", reconstruction done FOR the user) + exactly ONE suggested pasito, never a list. The suggestion is always explainable (kind → reason line), always overridable ("Otra idea" walks a deterministic day-stable cycle — `hash(today())` permutation, offset 0 always the ranked best), pool capped at 12, and NEVER suggests `resting` (paused on purpose), `achieved`, `branched`, or anything in archived trees. It never schedules the user's day and never counts refusals. The ranker is PURE (`features/ahora/suggest.ts`) — keep it free of Angular. Bucket order: **today's intentions** (≤3, `Settings.todayIntentions`, silently empty once the date moves on — no carryover, no history, no done/undone counts) → **when-then twigs** (`TreeNode.trigger`: the user's own if-then plan, free text NEVER parsed or scheduled — re-presenting it in Ahora/check-in IS the mechanism; alarms stay banned) → **«a pleno sol»** (`TreeNode.priority === 'sunlit'` — the user's standing light: below their explicit now, above everything ambient; reason «La tienes a pleno sol ☀️»; shaded branches skip the momentum echo and sort last in fresh buckets, with a quiet aside if one surfaces) → pasitos-of-thread → thread → 7-day momentum → fresh growing → seeds. `trigger` is an ADDITIVE optional field (SCHEMA_VERSION 2): old records lack it (`undefined ≡ null`), old backups import cleanly, no migration pass exists or is needed for it.
12. **Accompaniment doctrine.** Accompaniment exists at exactly four moments: **returning** (the thread card), **during** (the companion bird + the warm-ring bridge + the traveling perch), **right after** (momentum toasts at session-finish "Un pasito más" and step-bloom "¿Otro pasito?"), and the **whisper rhythm** (rule 7: opt-in orientation questions → check-in; in-app toast when visible, silent notification when backgrounded). Momentum offers ride the single toast slot: dismissible, auto-expiring, never stacked, never re-pushed. The SW is `sw.js` (importScripts-wraps `ngsw-worker.js` + handles whisper `notificationclick`) — registration path must stay relative.
13. **Companion bird constraints** (`features/timer/companion-bird.ts`): CSS-only animation with deterministic phase (`hash(sessionId)`), slow non-harmonic cycles (4s/6s/47s), poses are class swaps, ONE gentle hop max at overtime, fully static-but-present under `.reduce-motion`, renders ONLY while a session runs (no idle surveillance), never displays numbers or judgments, no sounds ever. The approach bridge (user-widenable: Settings.bridgeMinutes 2|5 since 0.0.68 — a hyperfocus exit-ramp; FocusSessionService.birdState is the ONE shared pose computed, bridge included — never re-derive it per surface) warms the ring toward gold — never red — and turns the bird; visual only. **The traveling perch** (`.session-perch` in `app.html`): during a live session the bird + elapsed time float above the tab bar on EVERY route except `/timer` and `/ahora` (which hold their own bird); tapping it returns to the session. The bird must never appear without a live session — presence means "your session is alive and I'm with you". **The bird is a PARAKEET (0.0.67, owner reference image)**: hooked coral beak + cheek spot are token mixes (rose+clay) — a SPECIES color, not an urgency signal; the class skeleton (.flip > .breathe > tail/body/belly/wing/wing-tip/.head(.beak/.cheek/.eye/.lid)/.legs) must survive redraws (all pose/animation CSS keys on it). **Scene perches (0.0.67)**: when the session NODE is on the open tree canvas, the perch anchors ON the ramita (.session-perch.on-branch — the note-letter overlay pattern: screen pos = tx + world·k, constant size through pan/zoom); when the session TREE stands in the visible clearing, it waits on the crown (.session-perch.on-tree — measured to the mini svg getBBox crown top, re-measured after the 0.35s plot glide; hidden mid-drag). Both claim via core/perch-anchor.service.ts and the app shell corner perch YIELDS — exactly ONE .session-perch exists at any moment (verify-perch counts on it; E/F assert the scene variants). Scene perches show bird + .perch-time chip, NO twig (real wood underneath). Corner fallback: hidden clearings, visits (repo lookup misses by design), «solo estar» sessions, mid-drag.

## Stack & architecture

Angular 22 (standalone components, signals, **zoneless** — no zone.js), SCSS design tokens, **zero UI/animation libraries** (hand-rolled by design: it's also the owner's portfolio evidence). TypeScript 6. Vitest scaffolding exists but the verification strategy is interactive (see Tooling).

```
src/app/
  core/
    config.ts   APP_CONFIG: backend 'mock'|'aws' + requireAuth + pool/api strings — the WHOLE migration flip
    db/       schema.ts (model+versions) · idb.ts (~100-line promise wrapper) · broadcast.ts (cross-tab + onLocalWrite)
    repos/    records.repo.ts (base: signal Map, write-through) · trees/nodes/checkins/sessions repos
              settings.service.ts (meta store) · backup.service.ts (export/import envelope)
    auth/     auth-types.ts (AuthUser/AuthError/PASSWORD_POLICY) · auth-provider.ts (seam token)
              mock-auth.provider.ts (offline Cognito twin, code 123456) · cognito-auth.provider.ts (lazy amplify + pool checklist)
              auth.service.ts (signals facade, fail-open hydrate) · auth.guard.ts (authRequiredGate, inert until requireAuth)
    sync/     sync.service.ts («conectar mi bosque»: opt-in watermark push + cursor pull, account.link + sync.state meta)
    api/      contracts.ts (NORMATIVE backend contract: RoadmapApi + shapes + paths + error codes)
              api-client.ts (seam token) · mock-api.ts (executable spec) · http-api.ts (fetch + bearer + retry)
              mock-cloud.ts (roadmap2u-mockcloud IDB) · mock-seed.ts (demo family: rocio/nico/val/ambar)
    i18n/     es.ts (Dict source) · en.ts · i18n.service.ts (signal lang, fill() templating)
    theme/    theme.service.ts (data-theme attr; ?theme= session override)
    motion.service.ts · time.ts (date-only helpers) · boot.service.ts (init + demo seed) · update.service.ts (SW toast)
  features/
    account/    account.ts (full-screen auth ritual: welcome→signIn/create→code→newPassword→profile)
    familia/    familia-card.ts (Settings section, signed-in only: create-minor w/ once-only temp password,
                per-child admin sheet + «Entrar a su bosque», coGuardian + linkExisting invites, code
                redemption, guardians view w/ the honest visibility disclosure; consumes
                core/family.service.ts — signals over GET /me, meta 'family.me' SWR, export-first deleteChild)
    amigos/     amigos-card.ts (Settings section, social-enabled only: friend code = the ONLY introduction,
                mutual-consent requests, SILENT declines, remove-confirm, visit entry; guardian oversight of a
                minor's friendships lives in the familia child sheet — same list the minor sees, removal never
                initiation). Friend visits are LOOK-ONLY: tree-view's canEdit() gates plant/review/node-sheet;
                taps and tablita locate instead of opening.
    visit/      visit-forest.ts (thin doorway: banner + MiniTree cards). The tree itself REUSES TreeViewPage
                under /visit/:userId — core/visit/ route-scoped repos (VisitTreesRepo/VisitNodesRepo shadow
                the real ones via route providers) reroute the WHOLE write funnel to POST /users/:id/sync/push
                (rev-LWW, guardian-only), so plant/branch/steps/date-review work on the KID'S cloud forest and
                never touch the visitor's IndexedDB (verify-visita F asserts 0→0). Visit gates in tree-view/
                node-detail via optional inject(VisitSession): neutral sky (feelings are private), no ⏳/🗃
                header, no session offers. Stripped (read-only) visits throw FORBIDDEN on writes — the
                affordance-hiding pass for friend visits lands with «amigos y visitas».
    ahora/      ahora.ts (HOME: thread card + ONE suggested pasito + session companion card) · suggest.ts (PURE ranker + thread resolver)
    check-in/   check-in.ts (ritual: weather → where → note → [date-review] → CIRCLE of trees) · date-review.ts
    forest/     forest.ts (meadow scene) · mini-tree.ts (real data miniatures) · tree-view.ts · tree-canvas.ts
                tree-layout.ts (PURE: layout, ribbons, hash) · scene-backdrop.ts (sky/mountains/mood weather)
                flora.ts (accent→flower species) · flower.ts (g[appFlower] SVG component)
    node-detail/ node-detail.ts (sheet) · branch-flow.ts (transformation + suggestion chips)
    timer/      timer.ts (thin view over FocusSessionService) · companion-bird.ts (single-player body double)
    settings/ · guide/ (in-app manual) · trail/ ("Tus huellas": check-ins + branch notes + sleeping branches, deep-links via /tree/:id?node=)
  shared/ui/  toast.service.ts

infra/          THE REAL BACKEND (own npm workspace — never in the Angular build graph):
  lib/roadmap-stack.ts   CDK: Cognito pool (per the adapter checklist) + DynamoDB `roadmap` (2 GSIs, TTL, PITR)
                         + HTTP API (JWT authorizer on idTokens, CORS for Pages+localhost) + outputs = APP_CONFIG strings
  lambda/       router.ts (route table mirrors API_PATHS 1:1 — parity-tested) · handlers/ (me·family·friends·forests·sync,
                authz matrix from the table, LWW conditional puts) · post-confirmation.ts — ALL import contracts.ts via @app alias
  test/         vitest + aws-sdk-client-mock (16 tests). Verify: `cd infra && npm test && npx cdk synth` — NO AWS account needed
```

### Data model semantics (`core/db/schema.ts`)

- **Naming history (2026-07-06): the app was renamed RodeMap2U → RoadMap2U, storage identifiers included** (DB `roadmap2u`, mock cloud `roadmap2u-mockcloud`, channels `roadmap2u-db`/`-auth`; the `rm2u.*` localStorage prefix abbreviates the new name too). Two LEGACY literals remain FOREVER and must never be "cleaned up": (1) `LEGACY_DB_NAME = 'rodemap2u'` — `openDb()` copies a pre-rename device's forest into the new DB once (COPY, never move; the legacy DB stays untouched as a safety net; verify-migration.mjs proves copy + frozen-legacy + writes-go-new-only); (2) backup import accepts `app: 'rodemap2u'` alongside `'roadmap2u'` (backup.service.ts) so every backup ever downloaded keeps importing. New envelopes are produced as `'roadmap2u'` in all three producers (backup.service, mock-api.exportChild, infra family.exportChild).
- Every record extends `SyncBase { id (uuid), createdAt, updatedAt, rev, deletedAt }`. Last-writer-wins follows the ONE shared ordering `contracts.lwwBeats` (rev first, `updatedAt` breaks equal revs, exact ties keep the SERVER's stored copy — servers reject, clients adopt the returned winner, so equal-rev replicas converge): servers (`mock pushInto`, DynamoDB condition) accept iff `lwwBeats(incoming, stored)`; clients (`acceptRemote`, `RecordsRepo.applyExternal`) reject only when the current copy strictly beats the incoming one. Outbound rides `onLocalWrite` (`broadcast.ts`) through a watermark scan PLUS a persisted dirty-id set (the clock-proof half — a backward clock jump cannot orphan writes; persisted DEBOUNCED on every mark since 0.0.65, not only after a successful sync); inbound lands through `applyExternal()` — the same hook BroadcastChannel uses today. **Two rails (0.0.65)**: `broadcastChange` = local writes (fires this tab's sync handler + other tabs); `broadcastRemote` = pull-applied server records (OTHER TABS ONLY — routing pulls through the local handler re-marked everything dirty and echoed a full re-push every round; the old `applyingRemote` gate that "fixed" this silently dropped genuine user writes mid-pull and is GONE). ⚠️ `RecordsRepo.save()` stamps rev/updatedAt; **`saveMany()` does NOT — every caller must `stamp()` its records** (unstamped `setOrder` shipped: synced reorders tied the server's rev and LWW reverted the drag two seconds later). `refreshFromDisk` DROPS ids missing from disk (import-replace removal must reach other tabs' memory). `today()` (core/time.ts) is REACTIVE — a module signal flips at midnight so day-keyed computeds follow the sun; never cache its value across turns of an effect. Toasts queue (`ToastService`): action-bearing toasts (undo) are protected — newcomers wait; the sticky update offer steps aside and re-surfaces. Focus sessions mirror `sessions.running()` cross-tab (an effect adopts/clears; `start()` adopts instead of double-starting) and PAUSE state persists on the row (`pausedAt`/`pausedMs` — a reload used to convert the paused span into "worked" minutes).
- `archivedAt` (user-facing, recoverable) ≠ `deletedAt` (sync tombstone, permanent). Queries filter tombstones in exactly one place (`RecordsRepo.all`).
- **Gentle dates are date-only strings** `'YYYY-MM-DD'`, compared lexicographically against local `today()` (`core/time.ts`). **NEVER** `new Date('YYYY-MM-DD')` — it parses UTC and shifts a day.
- Branch-on-miss is **structural and atomic** (`NodesRepo.branch` → one `putMany` transaction): parent → status `branched` (keeps its original `targetDate` as history), children born with `origin: 'branch'` (rendered with golden tint + knot). The tree shape IS the history; there is no event log.
- **`NodesRepo.revertBranch` is the only exit from `branched`** — undo of an unrooted transformation, atomic: parent lands `growing` + dateless by default (a preserved past `targetDate` must NOT re-arm date-review; the toast-undo in `branch-flow.ts` restores the exact prior record on purpose), untouched `origin:'branch'` children leave as sync tombstones (archived nodes are invisible in every UI — tombstoning is the honest shape). The quiet affordance in node-detail is gated on ALL branch children being untouched (`revertable` computed). `branched` still cannot be hand-picked.
- Passed-date predicate: `targetDate < today() && status not in (achieved, branched)`. All three review exits (keep-going clears the date / move / branch) end the predicate. Reviews live in Ahora's 🍂 banner and the tree-view banner ONLY (the check-in ritual never surfaces them since 0.0.39); archived trees' dates are excluded.
- **`TreeNode.flow` (SCHEMA_VERSION 3, additive like `trigger`)**: `'steps'` marks a branch whose pasitos are an ORDERED path; absent ≡ `'free'`. Never forced — the "¿Van en orden?" toggle lives in the node sheet. Steps are REAL child nodes (order asc = the sequence); `NodesRepo.moveStep` is the app's only node reorder (adjacent order swap, atomic). The ranker needs no restructuring: order-asc `childrenOf` already surfaces the earliest open step; it just carries kind `'step-in-order'` (reason "El siguiente paso de…", first→then footer on the Ahora card).
- **`TreeNode.priority` (SCHEMA_VERSION 4, additive like `trigger`/`flow`)**: «la luz» — `'sunlit' | 'shade' | null`, absent ≡ steady. Never initialized on plant/branch; cleared with `null`; `lightRank()` (schema.ts) is the ONE sort helper shared by ranker/tablita-lens/timer-picker/date-review. Full doctrine in the "Deliberately NOT built" section's luz entry.
- `Settings.lastCheckInAt` drives a 30-min cooldown on the check-in gate (route guard in `app.routes.ts`).
- Migrations: `DB_VERSION` (IndexedDB structure, `onupgradeneeded`) is separate from `SCHEMA_VERSION` (data shape, pipeline after open; import reuses it).
- Storage resilience: `openDb()` rejects after 3s and every repo degrades to **memory-only session** — a broken IndexedDB must never blank the app.

### Rendering system

- `tree-layout.ts` is **pure and unit-testable**: leaf-slot layered layout (root bottom, growth up), deterministic jitter, `taperedRibbon()` (sampled bezier + perpendicular offsets = filled tapered timber; NOT stroked paths), `edgeGeometry(parent, child, bowScale, opts?)` — miniatures pass `bowScale≈scale` or absolute bows read as seaweed; `EdgeOpts { upBias, bowMul, hand }` defaults reproduce the classic curve byte-identically. The bezier bow rides each limb's PERPENDICULAR (damped on shallow limbs) — never revert to x-only offsets (they S-warped wide fans).
- **Species silhouettes ("cada árbol su porte", 0.0.43; expanded 0.0.61)**: `tree-forms.ts` maps the accent → a `TreeForm` — SIX archetypes now: oak (moss/rose), acacia (clay/sand), slender (lavender), **willow** (sage — habit `weeping`: tip limbs hang via negative upBias, pads drape BELOW tips), **birch** (sky — habit `zigzag`: the leader hand alternates by depth, `barkTint:'pale'` washes the bark family), **conifer** (pine — habit `tiered`: side limbs sweep out-and-slightly-down, slimmer collars, flat wide pads). The 0.0.61 remap changed sage/sky/pine silhouettes exactly once (owner-consented). `formFor(accent, treeId?)` additionally JITTERS the dials per tree id (upBias ±0.08, leaderBias ±0.1, fork window ±0.06, padSpread/leafDensity ×~0.8-1.2, padCount +0/1) — same accent = same family, never twins; omit the id for the base form. Habits are RENDER-side only (`limbPlan` + mini echoes — minis pass per-edge upBias/hand since they don't run limbPlan); node positions never move. **LEADER choice (0.0.62 — the left-lean fix)**: mass carries the trunk only when CLEARLY heavier (>15% over every rival); near-ties go to the most CENTRAL child (`|c.x − parent.x|` min) so the trunk continues UPWARD; exact central ties break by `hash(tree.id + parent.id + ':leader')` — per-tree structural variety. Never revert to the plain `>` max (on tied masses it always kept the LEFTMOST slot: every fresh fork leaned left and every tree repeated one windswept silhouette — Hector's 0.0.62 complaint). **Node glyphs are species-colored (0.0.62)**: seed tints `species().petalEdge`; growing = the CAPULLO, a closed bell of the tree's own flower (petal body, petalEdge seam, heart mouth-sliver, green sepals; leans via `saplingAngle`) that "opens" into the species bloom on achieve; `resting` deliberately keeps universal lavender (pause reads the same forest-wide); `branched` stays the golden knot. Minis tint their growing bud dot with species petal (inline attrs — a stylesheet fill would override them). Shared base: (1) **width by carried mass** — `widthForMass(mass, girth) = clamp(2.4, 5.2·girth·√mass, 30)`; `LayoutPoint.mass` persists `place()`'s leaf count; `widthAtDepth` is GONE (trees thicken as they grow, babies stay stems); (2) **leader axis** — per parent, the max-mass child's edge continues the trunk (calm bow via `leaderBias`, opposite hand, w0 ≈ parent width; all in `tree-canvas.limbPlan`, render-side only — node positions untouched); (3) **side-forks** — non-leader children's WOOD starts at an anchor along the leader limb (`edgePointAt` at a `forkTMin..Max` t; collar clamped by `ribbonWidthAt`); (4) **upward curvature** (`upBias` mixes arrival tangents toward vertical); (5) **foliage pads** at tips per form (acacia = flat pads only with air; oak = round pads + leaves; slender = sparse). Chains (`flow:'steps'`) skip leader/fork logic — they already ARE a continuation, and the mass law tapers them along the path (deliberate). `mini-tree` copies `mass` in its scaled-point rebuild and shares `formFor` — big tree and minis must always agree.
- **Crowded canopies**: ≥4 siblings stagger across two sub-rows with a "vase" lift (outer limbs sweep upward, `TIGHT = 0.62` leaf advance so fans stop sprawling) and children that carry a crown rise an extra `CROWN_LIFT = 26` — the lift is computed BEFORE recursing so a crowned child lifts its WHOLE subtree (trees grow UP, not just sideways; cap 64). Parents center on the **leaf-mass centroid** of jitter-free child positions. (The pre-0.0.43 "demo golden look" was retired by owner request — his reference images are the new north.)
- **Labels: always visible, never overlapping, never "…"-cut** (0.0.59, `tree-labels.ts` — pure, vitest-covered). Every branch name wraps to a multi-line block and packs into ONE global occupancy field anchored on `rowY` (pre-jitter row line, lift included). THE LAW: the field never reads the zoom `k` or `focusedId` — visibility is identical at every zoom, and tabbing never reflows names (the focused node shows a full-title cartouche overlay instead). Crowded regions drop to a shared smaller tier (regional smoothing — one fan reads as one type size, no single name looks punished). Titles too long for a saturated fan descend a GRID-CHECKED shrink ladder and end with a soft fade-tail (`tail`), never an ellipsis, never hidden — the full name always lives in the cartouche, the tablita, the node sheet and the aria-label. Overlap is a hard invariant at every rung. Per-node `emphasis` reserves real width in the packer (the «luz» hook) — never set a label size the packer didn't reserve.
- **The "tablita"** (`tree-outline.ts` + `.outline-rail` in tree-view): a floating branch outline over the canvas' right side — it must stay an OVERLAY (never a layout column: reflowing the canvas would re-trigger fit math). One tap = locate (`TreeCanvas.focusNode`, public, pans into view), second tap on the same row = open the sheet; ordered-steps children show their 1./2./3. numbering. Browsing never opens sheets. Rows with children carry a ▸/▾ triangle; trees over `AUTO_COLLAPSE_AT = 12` branches open with depth-1 parents folded (roots stay open; a `(N)` chip counts the sleeping descendants). Collapse state is session-scoped by design — the tablita is a lens, not state.
- **The meadow is CLEARINGS ("claros", 0.0.43)**: `.plots` is a positioned band (`height min(400px, 50vh)`), each plot ABSOLUTE at a hand-tuned `ARRANGEMENTS[n]` anchor + the tree's own hash wobble — **anchors are seeded by ORDER-INDEX** (drag/Arrow reorder must MOVE trees; id-seeded scatter would kill that meaning). Feet stay in the meadow band (never the sky — that bug shipped once); z-index follows foot-y (painter's order); back rows hug the stream's NEAR bank via the dry-feet clamp (`bankLimitPct` samples the ribbon's real bezier at the live slice scale — plain percentages put trunks in the water on wide windows; that shipped once too). Pagination: 6 trees per clearing (3 under 700px — four grown crowns jostle at 390px, 0.0.63) with ‹ › arrows + dots; planting jumps to the newborn's clearing; a clamp effect keeps `page` in range; keyboard nudge may cross pages. **Cross-clearing drag (0.0.63)**: holding a tree against a 52px screen-edge zone flips the page (600ms throttle) and the drop lands in the new clearing. THE INVARIANT THAT MAKES IT WORK: while dragging, `pageTrees` APPENDS the carried tree to whatever slice is shown — if the dragged plot unmounted on a flip, the latched TOUCH stream would die SILENTLY (no pointercancel, events just stop; document listeners see nothing because the latched target left the tree) and the drag collapsed. Kept mounted, the `@for` (track id) reuses the node and the gesture survives; `slotFor` self-clamps the 7th/4th plot onto the last anchor. Never page-slice the dragged tree out mid-drag. Touch grab is forgiving: 300ms hold (mouse 250ms), 26px slide-cancel; a document-level `touchmove` `passive:false` lock prevents scroll while dragging. **Pointer-following drag (0.0.64)**: while dragging, the carried plot renders from `dragPos` (band-% position + FROZEN scale — `slot.s` is index-dependent and would snap on every preview splice) instead of its anchor slot; the template bindings are single-writer ternaries (`drag ? drag.x : slot.x`). Grab offset = pointer→bottom-center (with `transform-origin: 50% 100%` + `translateX(-50%)`, the rect's bottom-center IS the anchor). `dragPos` updates BEFORE the edge check so the tree keeps riding the finger while parked at the edge. `.plot.dragging` = `z-index: 300 !important` (must beat hover's 200) + `transition: none` + ground-shadow `::after`; the block must sit AFTER the `:hover` rules (source order breaks the ties). NEVER `pointer-events: none` on `.dragging` (breaks slow-click navigation under async zoneless CD and hands hover-z to the neighbor; `elementsFromPoint` already sees occluded neighbors below the carried plot). On drop, `.settling` (~520ms) springs the glide home + settle-pop; `dragPreview` stays alive until `setOrder` resolves (nulling it early flashes the OLD order under the glide). verify-meadow C2 asserts the carried plot's center tracks the mouse within 40px. **Pointer discipline (0.0.65)**: ONE pointer owns the gesture — `dragPointerId` filters moveOver/endMove (a resting palm's move must not teleport the tree; its lift must not commit the half-finished order), `plotDown` refuses while a drag/pending is alive and requires `ev.button === 0` (right-click used to grab), `beginDrag` clears `settlingId` (the later-in-source `.settling` transition beats `.dragging`'s `transition: none` on a quick re-grab → rubber-band) and resets grab offsets, `nudge`/`askArchive`/dots are inert mid-drag, settle timeouts carry a generation counter. Anchor tables must keep every plot's CENTER un-covered by taller neighbors (elementFromPoint at each center must resolve to itself — verify-meadow asserts it).
- **Earned size (0.0.46/47)**: meadow plots scale with content (`growthFor`: √(branches + 2·blooms), 0.78..1.22, capped 1.12 in crowded clearings) AND MiniTree's internal fit cap is content-driven (`min(0.68, 0.18 + 0.10·√content)`) — without the internal cap, a tall skinny sapling fills its viewBox and visually outgrows a wide worked crown (shipped once). verify-meadow H measures the PAINTED bbox, not the plot box.
- **Side-fork declutter (0.0.45)**: side limbs sort by horizontal REACH — the farthest-reaching forks LOWEST on the leader limb (real-tree tiering; unsorted anchors made same-direction limbs run as crossing parallel rails). Crowded forks (≥4 sides) widen the t-window and slim collars ×0.78. Branch-flow custom chips: `Settings.customBranchChips` (max 6), ☆ saves a typed path verbatim, ✕ removes — managed inside the flow only (finite config surface).
- **Ordered-steps chains** (`flow: 'steps'`): the siblings render as ONE climbing limb of short `CHAIN_H = 46` segments — `layoutTree` rewrites the traversal (parent shows only step 1; each step carries the next via `LayoutPoint.chainNextId`), so wood/leaves/flowers/nearest-pick all reuse. Achieved segments bloom (the path fills with flowers). Labels: chain links stay quiet except the NEXT open step (+ focused/📍); labels anchor on `LayoutPoint.rowY` (never assume `-depth·LEVEL_H`). The focused-node "+" bud shifts diagonal on steps parents (straight-up lands exactly on link 1).
- **«Respirar» (0.0.75, density audit)**: the ONE-suggestion card renders AT MOST one secondary line (ahora.secondaryLine — priority first-then > shade aside > estimate whisper; never stack justifications again); the 🍂 review banner is an inline pill; the node sheet folds 🕐 estimate + 🧶 trigger behind «Más detalles» (moreOpen; collapsed chevron shows 🕐/🧶 marks when they hold values) and the luz explainer shows only while the node has NO priority (teach once); the check-in regadera folds like the notita (energyOpen — optional extras share one expander shape). EMOJI-ICON LAW: one glyph = one meaning — ⛱️ is shade (matches the canvas parasol; 🌳 means forest/tree ONLY), 🕐 is time-estimate (🧭 means orientation/thread ONLY). Check collisions before assigning any new UI emoji.
- **«La promesa» (0.0.93, ADHD-patient ask — frascos prometidos / goal jars)**: the conservería now runs FORWARD too — a jar can be created EMPTY, ahead of its fruit, and filled by completed tasks («mientras más grande el regalo, más grande el envase»). Clinically = a prospective self-administered CM contract + the externalized permission slip made concrete BEFORE the work. Data (SCHEMA_VERSION 8, additive on Preserve, DB_VERSION stays 3): `plannedAt` (birth at the wizard; absent ≡ null = an ordinary pot jam, never a promise) + `sealedAt` (absent ≡ «sealed at madeAt» = legacy pot jams are born sealed; a promise carries null while filling). **Pending ≡ `plannedAt != null && sealedAt == null`** (pure predicates `isPromise/isPending/isSealedJam` in harvest.ts; `jarCapacity` = the TOP of each published band: frasquito 2 · frasco 5 · frascote 8, so a full auto-sealed goal jar re-derives its own `size` and is indistinguishable at rest from a pot jam). NO capacity field (derived from `size`, which the USER picks in the wizard = their OWN valuation of the premio — the app NEVER suggests reward↔size). TWO JAR FAMILIES now share the store: *frascos de la olla* (size from the fruit, past-facing) vs *frascos prometidos* (size chosen forward). AMENDED «la app es la alacena, nunca la caja registradora»: the ONE forward count line («lleva n · le caben cap») is an owner-sanctioned carve-out that lives SOLELY on a pending jar's detail panel — never on shelves, the mesita, or the pot (verify §X grep; the anti-quota §N pot grep must still pass — the wizard is a separate door). AMENDED season law: only a SEALED home is immutable — a fruit in a still-PENDING jar re-stamps in place (recordBloom now injects PreservesRepo: re-stampable = fresh OR placed-in-a-pending-jar; only sealed rows mint `:sN`). Engine primitives in `conserveria.service.ts` (`promise/place/unplace/release/editPromise/unfill/reconcileSeal`) + feature `promise.service.ts` (owns the palette — flora `jamTint` — so core stays feature-free; ≤3 pending via `MAX_PENDING_PROMISES`; `placeAndCelebrate` centralizes the one celebration). AUTO-SEAL is the app's FIRST self-initiated transition — but it only changes HOMES/stamps, never MINTS: `place()` seals in the same `putAcross` when its own placement reaches capacity → the MODEST celebration (a toast «Se llenó tu frasco «X»» with Deshacer = `unfill`; the 14-petal rain stays RESERVED for opening — llenar→sellar is small, abrir→reclamar is the peak; Zeigarnik gap between). CONVERGENCE: `PromiseReconcilerService` (effect in App, off-effect microtask, guarded by sealedAt) silently seals any pending jar already ≥capacity from a sync merge (deterministic stamps: sealedAt=madeAt=max member harvest — no `now`; ceremony fires ONLY on the device that placed locally, so no double celebration). Placement is single-home (`preserveId` → the fruit leaves `harvests.fresh()`, so a promised fruit can't be pot-jammed) and reversible until seal (`unplace`); «soltar» (`release`, confirm-sheet `quiet`) frees fruits + tombstones — a goal jar NEVER locks anything, «soltar» always available (fruit is never currency/gating). ENTRY: bloom toast in node-detail (`offerToStore` — 1 jar = direct `placeAndCelebrate`; 2–3 = `requestPlacement` → cross-page `PlacementPicker` mounted in App) + manual «Agregar fruta» tray on the pending detail; both gated to non-visit (visits never mint, so the toast never fires; picker never reachable). RENDER: `forest/promise-jar.ts` (chosen glass silhouette via `glassFor` + member fruits inside via shared `placeFruits`, neutral glass, NEVER a jam liquid or a fill line); sealed goal jars join the alacena as normal mermeladas (🎀 always, premio required) and reuse the EXISTING abrir ceremony untouched. Mesita: a pending jar leads (motivational), cap 2 total. Wizard `promesa-sheet.ts` (@defer, appSheet, doors-cancel, global `.chip.selected`, 3 beats: premio REQUIRED → vessel choice with capacity in words+number → editable name); door hidden at 3 pending. verify-conserveria T-Z + verify-migration v7→v8 + verify-backup v8.
- **«El premio del frasco» (0.0.90, owner same-day v3 ask — jam as self-granted reward + three vessels)**: clinically = SELF-ADMINISTERED CONTINGENCY MANAGEMENT (magnitude scaling is a core CM principle — Petry 2004; reward-planning is a module of validated adult-ADHD CBT — Safren) and, deepest, the EXTERNALIZED PERMISSION SLIP: the past self, standing on visible evidence, writes the future self a permission it can't otherwise feel it deserves (an inverted Ulysses contract — binding TOWARD joy; the ADHD burnout/guilt literature is the reason). THE ROT LINE, one law: **«la app es la alacena, nunca la caja registradora»** — the app may state what a finished batch NEEDS, never what a future reward COSTS; NO forward-looking arithmetic linking count to reward anywhere (no pot counters, no tier previews, no «te falta N», no size-sorted shelves, no expiry, no locks); magnitude YES, CM's reset-contingency NEVER (a streak with punishment). Data (SCHEMA_VERSION 7, additive on Preserve): `size` (JarVessel snapshot at seal — never live-recomputed; pre-v7 jars = frasco forever), `premio` (the user's OWN words, trigger-law: never parsed/valued/priced/scheduled/suggested — as intimate as trigger, strips FIRST if preserves are ever served), `savedFor` (optional intención — re-spoken in the panel, NEVER enforced, NEVER shown at opening: no «you said you'd wait» energy exists anywhere; severable if the psychologist balks), `openedAt`. **«El frasco sirve a la fruta»** (owner override of same-size, recorded like «la luz»): `jarSizeFor` = THE one threshold law (1–2 frasquito · 3–5 frasco · 6+ frascote); thresholds PUBLISHED in the guide (autistic predictability — a rule you can read) but NEVER computed forward on a working surface (ADHD goal-gradient — never a bar you can watch); the tier speaks ONCE, at Envasar, past-facing («tus N frutas piden un frascote — una mermelada poderosa»); vessels are three distinct SILHOUETTES in jar.ts (FRASQUITO/JAR/FRASCOTE_GLASS_D — a shelf of scales is a histogram, a shelf of shapes is a pantry; a frasquito is its own charming kind, never a lesser frasco — «ninguna mermelada es chica» survives as vessel-identity); alacena stays chronological, never size-sorted/counted. **🎀 = premio** (emoji law): a bow on the cap-tie marks a gift inside — orthogonal to size, never a count. Premio field at beat 3: optional + quiet (absence never named — «sin premio» appears NOWHERE), placeholder = exactly TWO fixed first-person whispers forever («un maratón de mi serie», «una tarde libre, sin culpa» — one treat one permission, the second teaches that rest counts; NO reward menus/suggestions ever — the app never values rewards). **«Se abre una vez; se conserva siempre»** (replaces sealed-finality for premio jars): «Abrir la mermelada» (abrir-mermelada-sheet.ts, @defer) = breath (doors cancel; «Todavía no» first-class; no date checks, no earned-ness questions, intención not shown) → claim (openedAt stamps; the 14-petal rain falls in the JAM'S OWN TINT via HarvestSkyService.rainTint — no tarjetita, the sheet is the card; NO escalation by vessel) → «Te lo ganaste:» + the premio VERBATIM + savoring (té DNA, member sip-cards). The OPENED jar renders «disfrutada» (lid resting behind the shoulder, bow undone, liquid 0.65 — enjoyed like a lit candle, NEVER greyed/spent-looking; memories re-readable forever; fruits still té-pickable; the open button simply gone — nothing to farm, the reward happened outside the glass) and — amended 0.0.92 «las disfrutadas», owner ask — MOVES from the alacena to its own quieter history shelf below («Las disfrutadas», sorted by openedAt, 0.85×, never counted): the alacena stays the shelf of WHAT WAITS (sealed jars only; memory jars live there forever since they never open), history is a move never a deletion (nada se gasta), and the mesita prefers SEALED jars (enjoyed ones fill in only when fewer than two wait — the corner advertises what waits). The undo window (reclose) returns the jar to the alacena automatically. Undo toast windows for BOTH seal (unseal) and open (reclose), both labeled «Deshacer» — *abrir* means only the ceremony now (i18n `openJar` retired). The app NEVER locks/expires/nudges a jar (no «sin abrir» states; whispers never ask about premios; opening early is never remarked). Designed-not-built relief valve recorded: one-time «cambiar el premio» edit IF the psychologist observes stale-premio avoidance. Memory jars (no premio) gain nothing and stay complete («algunas mermeladas guardan un premio; otras guardan un recuerdo — las dos están completas»). verify-conserveria L-Q (frasquito+ribbon+fields · memory-jar-unnamed-absence · ceremony+stamp+Deshacer-recloses · reduce-motion words-stay-sky-aside · anti-quota grep-absence in the pot · six-fruits=frascote-poderosa).
- **«La conservería» (0.0.89, owner same-day v2 ask — full-screen celebration + jam batches + té)**: MASTER LAW: **«Nada se gasta; todo se conserva»** — a transformation may change a fruit's HOME (`Harvest.preserveId`, the single-home field: absent/null = fresh jar, set = one sealed batch) and its FORM, never its existence or visibility; the REGISTER (`/cosecha` month shelves) shows every fruit forever (jammed rows wear the «en “{jarName}”» chip — single-home made visible), every jar opens to its members' memory cards, and the ONE lifetime count counts the register so it CAN NEVER DECREASE on sealing (monotonicity = the user-visible proof nothing was spent). Data: `preserves` store (SCHEMA_VERSION 6, DB_VERSION 3; Preserve = kind/name/madeAt/accent-or-null/tint snapshots; ids randomUUID — every seal is a new batch) + additive `Harvest.preserveId`; membership lives ON THE MEMBER (never an array on the batch — LWW conflict-free); seal/unseal = ONE `putAcross` transaction across both stores (idb.ts helper; stamp() before the write — saveMany never stamps) via `core/conserveria.service.ts`; set ONLY by the ritual, never by sync/import/backfill. **SEALED FINALITY** (amended 0.0.90): the SEAL's undo lives in the toast window only and says «Deshacer» (preserves don't un-cook; make/unmake is an ADHD fiddle-loop) — *abrir* now belongs exclusively to the claiming ceremony (see 0.0.90). THE RITUAL («Hacer mermelada», features/cosecha/mermelada-sheet.ts, @defer, DOORS CANCEL at every beat): pot has NO minimum («con una alcanza — ninguna mermelada es chica»), NO cap, NO counter, NO select-all (one-by-one picking IS the reminiscence re-read); NO FLAVOR MENU ever — the label derives from the hands (one species names itself via `deriveAccent`, mixed → «Mermelada del bosque», FIRST-CLASS, prettiest blend via `jamTint` numeric hex average); stirring is play never a gate («Envasar» enabled from the stove's first frame); the jar name is prefilled + EDITABLE (the user's words, like branch titles). Jam jars (`forest/jam-jar.ts`; the glass law is now a closed THREE-VESSEL family — see 0.0.90 «el frasco sirve a la fruta», owner override of same-size) keep THE SAME FULLNESS always (every vessel renders full of its own jam — quantity is never a fill line); jars are never counted anywhere; mixed jams show pure tint (single-species silhouettes would lie); «la alacena» renders only when non-empty, chronological. «LA MESITA» (meadow): the corner still-life = fresh jar + 2 most recent jam jars (0.6×), same anchor/z 60/one link — the corner only ever GROWS (the structural answer to post-jam robbed-jar guilt); fresh-jar-after-batch copy says READY, never empty. CELEBRATION per-layer equal dignity (refines 0.0.88): ACT layer = the small burst, every bloom, every site, identical; HARVEST layer = `shared/ui/harvest-sky.ts` (ONE service owns both so the mint-only law lives in one place) — «la lluvia» (14 FIXED petals, fixed lefts/delays/drifts/spins forever, z 690 above sheets below burst, ≤2.45s, compositor-only, replace-don't-stack — rapid blooms restart, never densify) + «la tarjetita» (z 705 top-center, FruitGlyph + per-species headline Record<FruitShape,string> + branch title; NO buttons EVER — never competes with the toast slot; tap-to-dismiss; 2.4s; **WEATHER, not a record**: never listed, never re-viewable, never varies beyond species+title) — both fire ONLY when `recordBloom` returns a row (stones/visits/sendero-pasitos = burst only; the sky never lies). REDUCE-MOTION: no full-screen anything (sky display:none); the tarjetita STAYS (it is information) with a plain fade. «EL TÉ» (te-sheet.ts): pick 1-3 fruits of ANY home or «Que me sorprenda» (hash(today()) day-stable); sips re-read memories incl. the branch's live notita; **NO residue** — no record that a tea ever happened (some joys are for drinking; that absence is what makes mermelada's permanence special). Elixir gates: E1 daily sendero distillate = a streak with amnesia, PERMANENT psychologist gate; E2 «elixir de despedida» (0.0.90 design ready: archiving a fruited tree distills a vial named by the tree) triggers ONLY on user-initiated archive, never suggested as a reason to archive. verify-conserveria.mjs (A-K) + verify-migration §E extended to v3 + verify-backup F1 v6.
- **«La cosecha» (0.0.88, ADHD-patient ask — fruit celebration + PERSISTED harvest jar)**: when a branch blooms, TWO celebration layers fire at the instant (dopamine timing is the point): «el estallido» (`shared/ui/bloom-burst.ts` — foreground petal burst at the TAP POINT in the tree's flower palette, host in app.html at z 700 above sheets; FIXED angles, identical for every bloom forever — equal dignity, no escalation, no combos; reduce-motion = one soft ring fade; fires at ALL THREE bloom sites incl. sendero stones — it celebrates the ACT) and «cae la fruta» (canvas-owned STATUS DIFF in the bornThisSession effect — a transition INTO achieved drops the species fruit from the tip in a one-shot 2.6s arc to the roots; outer `.fruit-drop` group animates OPACITY ONLY, its attr-transform carries position; reduce-motion = static soft-appear at rest; plays behind the translucent sheet backdrop, never deferred to sheet-close). The **status picker celebrates too now** (it was SILENT — same act, different control, no celebration = predictability wound; picker = its own undo, so a plain toast). FRUIT = 8 species in `flora.ts FRUTAS` (accent → KIND anchored to the FLOWER family hexes, not the --accent tokens; 3 tint cousins via `hash(id+':fruta')`; `g[appFruit]` sibling of FlowerGlyph) — **NEVER a fruit SET in UI** (rewritten 0.0.89: a species word may voice the SINGLE thing in hand — the tarjetita headline, a jam jar labeled by its own contents — but the kinds are never enumerable from the UI: no flavor menus, no species grids, no empty slots, no «te falta»; shelves chronological; mixed batches first-class; autistic set-completion pressure is the reason). THE PANTRY REGISTER (owner decision 2026-07-14, chose persistence over the lens): `harvests` store (SCHEMA_VERSION 5, DB_VERSION 2, full sync/backup/infra fan-out; deterministic id `'h:'+nodeId`; snapshots title/treeName/accent; **season law amended 0.0.91 «segunda cosecha»**: a FRESH row re-stamps in place — one USABLE fruit per branch, toggling never farms — but a row SEALED into a jam is IMMUTABLE HISTORY (a sealed jam's contents never mutate — the pre-0.0.91 re-stamp inside sealed jams was a real bug the owner's single-home review surfaced): re-achieving a jammed branch mints a NEW season row `'h:'+nodeId+':sN'` (N deterministic from row count — concurrent devices converge by LWW) into the fresh jar; the register accumulates both honestly, used fruits stay locked in their jams, and «no puedes volver a usar frutas ya usadas» is structural: the pot tray reads `harvests.fresh()` only, while the té deliberately reads ALL homes — savoring never consumes) — a fruit is a MEMORY and **survives reopen/archive/delete** (like Trail footprints), while the almanaque/meadow keep showing what STANDS: two registers, one voice each, documented — never "fix" the difference. Minting ONLY at the user-action layer (node-detail bloomStep + picker via `recordBloom`; sync/import/sweep/visits NEVER mint — guardian visits burst but the pantry is the owner's). **Sendero pasitos bear no fruit** (`core/harvest.ts isDailyPathParent/underDailyPath` — ONE law shared with `almanac.ts senderoStepIds`; daily durable fruit = a streak in a costume; the sendero PARENT retiring bears one with honor; the stone toast stays fruit-silent so the jar never lies). Backfill: `harvests.repo.ts backfillIfNeeded` (meta sentinel `harvests.backfilledAt`, sealMigration pattern, runs post-demo-seed in boot) seeds fruit for standing achieved branches — a lived-in device wakes with its pantry. «EL FRASCO» (`forest/jar.ts app-meadow-jar`, self-styled): glass in the house stroke, 12 most recent fruits at id-stable hash positions, fullness = CLUSTER DENSITY (no fill line, no number on the meadow); bottom-left of `.plots` at z 60 (wins taps over resting plots 10-50, under hover 200/perch 220/drag 300); **appears with the FIRST fruit** (stream precedent — no empty vessel staring; owner choice) and wiggles once on session arrivals (`jarCue` diff; no cross-session watermark, NO badges, NO arrival notifications — pull-based forever). `/cosecha` page (trail-shaped, jar hero + month shelves «La cosecha de julio», rows deep-link ONLY while the branch stands — a let-go branch keeps its memory, loses its door): ONE modest lifetime count line max («{n} frutas en el frasco», the sign-🌸 precedent) — **never windowed, never charted, never averaged**; NON-NEGOTIABLE: fruits are never spendable/exchangeable and nothing is ever gated on fruit count (transformations law rewritten 0.0.89 — see the conservería bullet: a transformation may change a fruit's HOME and FORM, never its existence/visibility; «hojas de té» for senderos ONLY with psychologist sign-off). Emoji law: 🫙 = la cosecha. Known accepted asides: sign «· N 🌸» counts today's sendero blooms while the jar doesn't (sign ≥ jar possible); pre-v5 backups import an empty pantry (the backfill sentinel is device state — restored achieved branches without harvests stay fruitless until re-achieved, honest). verify-cosecha.mjs (A-H incl. reopen-keeps-fruit + stone-never-mints + reduce-motion) + verify-migration E (lived-in v1→v2 upgrade + backfill) + verify-backup F (envelope round-trip).
- **Almanaque laws refined (0.0.82, sweep fixes)**: (1) `--rm-bark/--rm-twig/--rm-grass` are GLOBAL theme tokens now (styles.scss, both themes) — forest scopes re-declare the same values; any lens outside the meadow may paint wood/grass (the caminito stones shipped transparent for a day). (2) `setStatus` CLEARS `achievedAt` when reopening (seed/growing/resting) — a stale bloom stamp painted phantom flowers; `marksFor` additionally gates flowers on `status === 'achieved'` (branched shows ONLY its knot). (3) «Descansando» PAUSES a sendero: caminitos AND the daily sweep require a live parent (seed/growing) — yesterday's blooms stay put while resting; achieved retires, branched dissolves (and a branched former sendero STOPS hiding its children's marks; sub-steps hide recursively). (4) «Hoy quiere florecer» — branches dated exactly today get their own Hoy row (they are in nobody else's list: 🍂 is strictly past, upcoming strictly future). (5) The month view signal is null-means-follow-today (midnight month-flips move the grid unless the user pinned ‹ ›). (6) WIDE cells (the app's first min-width query, ≥720px) speak in TITLED CHIPS (.alm-chip: 13px species glyph + branch title ellipsis + 🍂 suffix if passed) washed in the tree's OWN accent — color-mix categorical, NEVER count-scaled (anti-heatmap); knots wash golden; narrow keeps the glyph svg; ONE priority order via pure dayChips (flowers > knots > capullos, cap). Grid tracks are minmax(0,1fr) — nowrap chip text otherwise forces 1fr wider than the container. (6b) «La página del día» is an inline DISCLOSURE panel under the grid (Apple agenda pattern; owner 2026-07-13): no backdrop, no focus steal, second tap or Escape closes (Escape bound on the section, never document — appSheet stack), aria-expanded/aria-controls on cells, dashed .selected ring (today's solid wins); appSheet deliberately absent (rule 8 governs MODAL sheets); ?day=YYYY-MM-DD deep-links a day (reserved for the future week strip). (7) Cell glyphs SCALE with the cell (svg aspect-ratio 40/20, max-height 64px — fixed-height marks were unreadable on desktop) and the grid carries a visual LEGEND (.alm-legend: flor/capullo/nudo/🐾/🍂). (7) Grid a11y: cells carry localized aria labels (dateLine), ‹ › use prevMonth/nextMonth keys, out-of-month cells are tabindex -1 and skipped by the roving arrows.
- **«El almanaque» (0.0.81, owner + TEA friend + psychologist ask — the TIME LENS)**: `/almanaque` (features/almanaque/; pure brain `almanac.ts` + spec) is a LENS over existing data, sister of the tablita — it MIRRORS, never schedules, never suggests (dates never feed the Ahora ranker: `suggest.ts` never reads `targetDate` — that is a law now). Doors: **the 5th TAB** (owner reversal 2026-07-13 of his earlier no-5th-tab call, after real use — wheat-spike glyph in the house stroke style, middle position Ahora·Bosque·Almanaque·Enfoque·Ajustes so the edge tabs never move; tabbar is a 5-col grid now; NO badge on the tab, ever) + Settings link; the forest-header pill was removed with the tab's arrival (header density). GOLDEN RULE (owner variant 2026-07-13): the past shows what OCCURRED — flowers on `achievedAt` days, golden knots on `branchedAt` days, neutral 🐾 on check-in days — and a passed-unresolved fecha amable KEEPS its capullo on its own day wearing a soft 🍂 (predictability > hiding; tap → the same dignified conversation; never red, never «atrasado»). Empty days = «barbecho», named as rest. SENDEROS leave NO month marks (their sweep erases achievedAt — painting vanishing history breaks predictability); they live only in «el caminito de hoy» (TEACCH stone strip: species flower on bloomed stones, «siguiente» tag reuses node.nextStepTag, tap-to-bloom with Deshacer, NO fractions, stones never dim by hour, unwalked wakes clean). No weather/energy on the grid (feelings live only inside the day page — regadera law); today's ring is GREEN («estás aquí» — gold stays knot/luz). The almanaque joins Ahora + tree-view as a third sanctioned PULL-BASED home of the 🍂 review banner (rule 9). Emoji law: 🌾 = almanaque. v2 backlog lives in plans/lexical-sparking-aurora.md («la siembra»: plant-date-from-day, brisa de mañana opt-in, piedritas daymarks store; v3 post-AWS: guardian ICS subscription, TRANSP:TRANSPARENT, zero VALARM, STRIP content; external calendar import as a feed: NEVER).
- **«De una pieza» (0.0.80, owner screenshot — trunk/ground junction)**: the trunk, its root flare and the first limb must read as ONE tree. Laws: (1) the flare is PROPORTIONAL (`trunkDims` base ×1.5, capped 12..40 half-width) and centered on the trunk's REAL swayed foot (`trunkFootX` — the old fixed 44px hump sat on root.x while the foot landed beside it); (2) trunk + flare wear the tree's own bark via `trunkFill` (= `woodFill(root)`) — a raw `--rm-bark` fill seams against per-tree limb colors; (3) they render as SIBLING paths with the same fill, NOT one merged `d` — merged subpaths with opposite windings cancel under fill-rule nonzero (a pale hole at the overlap; shipped briefly during this fix); (4) `trunkDims`: every planted branch thickens a young trunk (`effMass = max(mass, 1 + (n-1)·0.6)` — mass counts LEAVES, so the first child used to leave the trunk newborn-thin), and the top is always ≥ the mass width so the leader limb emerges from INSIDE the trunk (collar, never a step up). The 0.0.78 wood byte-equivalence trick no longer covers trunks (intentional change); limbs remain untouched.
- **«Sin astillas» (0.0.79, bug-sweep fixes — laws distilled)**: (1) DOORS CANCEL — dismissing any door (backdrop/Escape) must be a pure no-op; only explicit buttons commit (the first-pasito door used to START a session on dismiss). (2) IMPORT restores PREFERENCES, never DEVICE STATE — `importReplace` excludes `lastBackupAt/lastBackupNudgeAt/lastCheckInAt/lastWhisperAt/whispersEnabled` (the whispers toggle is THIS device's permission gesture; a stale lastBackupAt would clobber the pre-import copy's stamp). (3) The senderos sweep filters by LIVE TREES (`trees.active()`) — tree-archive doesn't cascade to nodes, and `nodes.visible()` alone leaks archived-tree nodes to any sweep; check both layers in every new bulk pass. (4) STRIP includes `estimateMin`+`repeatsDaily` (contract + mock + Lambda + infra test) — time guesses and routines are as intimate as `trigger`/`priority`. (5) Backup nudge: skipped when `sync.link()` exists (the cloud IS the copy), quiet on `/check-in|/account|/visit`, ARM-FIRST (no stamps at all → stamp `lastBackupNudgeAt` silently; never scold an old-but-new-here forest), and its toast carries `yields: true` — a yielding toast steps aside for ANY newcomer and re-queues (an Undo never waits behind an offer). (6) Corner perch z-index 400 — above content/tab bar, BELOW every sheet (500+): ambient UI never covers a modal. Known-benign, left alone: one-frame corner+scene perch overlap on scene-perch mount (effect-vs-render ordering); check-in's replacement-style expanders carry no aria-expanded on purpose (the button unmounts when open).
- **«La poda» (0.0.78, god-component extraction)**: (1) `tree-silhouette.ts` is THE silhouette brain — `planLimbs` (leader law + fork anchors + habits), `woodFor/woodFill/trunkPath`, `leavesFor/padsFor`, `groundDecorFor` — PURE and vitest-covered; tree-canvas.ts keeps only thin computeds wiring signals in (the tip predicate is the one live coupling). Equivalence was PROVEN on ship day: demo-guitar's full svg path data byte-identical live-vs-refactor (everything is hash-deterministic — reuse that trick for any future silhouette refactor). Touch limb math ONLY in the pure module. (2) `meadow-scenery.ts` holds the meadow's static fixed-seed scatter (GRASS/BUSHES/…/STREAM_PATH); live pieces (flowers-per-bloom, hasStream, mood) stay in forest.ts. (3) The finder sheet is `finder-sheet.ts` behind `@defer` (matching stays pure in finder.ts). (4) `shared/ui/perch-body.ts` is the INSIDE of every session perch (bird + time chip, `:host {display: contents}`, variant styles via `:host-context(.on-branch/.on-tree)`, corner twig via `[twig]`); the `.session-perch` anchors + positioning strategies stay per-surface, class names preserved for verify-perch. Deliberately NOT extracted: pan/zoom and plot-drag machinery (state-bound to canvas rect / plot DOM / paging — moving them changes wiring, not risk).
- **«Tu bosque, a salvo» + toggle law (0.0.77)**: (1) BACKUP REMINDER — `core/backup-reminder.service.ts` offers ONE toast per ~30 days (counting from `Settings.lastBackupAt` OR `lastBackupNudgeAt`, else the oldest live tree's age; `BackupService.download` stamps the copy). Opt-OUT via `Settings.backupReminders`; never over the check-in ritual; the data card shows «Última copia: …». Do not turn this into a badge, a red state, or a repeat nag — one line, then silence for the full cycle. (2) TOGGLE LAW — booleans render as `<app-switch>` (shared/ui/switch.ts, self-contained styles, `buttonId` keeps outer `<label for>` working, probes assert `aria-checked`); chips are ONLY for choice groups (theme, rhythm, sizes); checkbox-labels stay for in-sheet form options («¿Van en orden?»). Never a ✓/— chip for a boolean again. (3) `inputValue($event)`/`inputEl($event)` (shared/ui/dom.ts, exposed as protected fields) replace the `$any($event.target)` template idiom — new templates use them. (4) `<app-confirm-sheet>` now carries its OWN styles (feature-scoped `.sheet-backdrop` never reaches a shared component's DOM — 0.0.76 shipped unstyled dialogs; any new shared overlay component must bring its styles along) plus `quiet` (soft leave-taking: cancel stays primary — amigos remove) and `confirmDisabled`. Hand-rolled confirms remaining ON PURPOSE: the crumble wizard and ahora's first-pasito door (they are micro-FORMS with inputs, not confirms) and familia's info sheets (single Done button).
- **«Cimientos» (0.0.76, code-health audit)**: (1) CHIP LAW — `.chip` interactivity and `.chip.selected` live GLOBALLY in `styles.scss` (border + `color-mix` primary wash); never re-declare a local `.selected` for chips — six independent copies is how 0.0.73 shipped invisible selections. Feature scss may only ADD to the selected look, never carry it alone. (2) `<app-confirm-sheet>` (`shared/ui/confirm-sheet.ts`) is THE confirm dialog (icon/title/body/confirm/cancel + projected extras; appSheet focus-trap + Escape built in) — node-detail archive/revert and settings delete-forever/mock-reset use it; the crumble wizard (multi-step) and the forest/tree-view archive + ahora trail confirms are still hand-rolled (migrate opportunistically, keep `.sheet.card.confirm` classes — the verify battery selects on them). (3) `/tree` sheets load lazily: TreeOutline/NodeDetail/DateReview sit behind `@defer (when …)` in tree-view.html — keep new sheet-only components deferred too. (4) Pure-brain vitest: `core/core-brain.spec.ts` (lwwBeats/birdState/time), `features/ahora/suggest.spec.ts` (buckets, bajita partition, pickAt, resolveThread), `features/forest/finder-flora.spec.ts` — grow these when touching the ranker. — TEACCH visual-schedule shape, owner-approved over the explore-first caveat; ship QUIET, no kids-feature promotion until the psychologist validates)**: optional TreeNode.repeatsDaily on a steps parent («Se repite cada día», toggle under «¿Van en orden?»). core/daily-paths.service.ts sweeps on the REACTIVE today() (boot + midnight): steps achieved on a PREVIOUS day reset to seed (achievedAt null, one stamp) — today's blooms are NEVER touched (idempotent within the day). NO history, NO streaks, NO completion counts. verify-senderos.mjs backdates achievedAt via IndexedDB to exercise the sweep.
- **«Brújula del tiempo» (0.0.71, clinical audit — time blindness)**: optional TreeNode.estimateMin EstimateMin|null (2|10|30|60|1440|10080 — schema ESTIMATE_CHOICES; labels via estimateSizes dicts, NEVER raw minutes past an hour); curiosity line fires only for session-scale guesses (≤60) («ni idea» is a first-class chip; re-tap clears). The suggestion card whispers it (ahora.estimateHint). The curiosity line is OPT-IN (Settings.timeCompass, default OFF — time feedback can shame): FocusSessionService.maybeTimeCompass fires at most ONE line per finish, only on a NOTABLE gap (≥2× and ≥5 min), DEFERRED 80ms so the caller's momentum toast owns the slot first (the plain-toast replace rule would kill it otherwise). Never charted, never averaged. verify-brujula.mjs (probe gotcha: read the toast BEFORE dismissing — a plain momentum toast means the curiosity is ALREADY current).
- **«La regadera» (0.0.70, clinical audit — AuDHD energy accounting)**: optional CheckIn.energy 'llena|media|bajita' token on the feeling screen (tap-to-toggle, never advances the step, never required). GUARDRAILS mirror «la luz»: never a score, never charted, never compared across days, only TODAY's check-in counts (todayEnergy computed in ahora.ts). Bajita → suggestionPool does a STABLE PARTITION (never a filter): explicit-now kinds (today/trigger) stay first, then CHILD leaves (concrete pasitos — a bare TOP-LEVEL goal has zero children too but is the big ambiguous thing, NOT small), then the rest; floated smalls carry Suggestion.lowEnergy → reason «Para un día de regadera bajita, un pasito chiquito 💧». verify-regadera.mjs.
- **«Desmenuzar» (0.0.69, from the clinical audit — task paralysis)**: node sheet 🔪 CTA while a LIVE branch has ≤1 pasitos — three compass questions (first physical action · what stands in the way · the 2-minute version), each non-empty answer planted as a pasito IN THE USER'S OWN WORDS (cuando-entonces philosophy: no AI, no templates); every question skippable, skip-all plants nothing. Ahora's 2-minutitos on a BARE top-level goal asks ONE first-pasito question first (answer planted + session starts ON it; «Empezar así nomás» keeps the old path — a door, never a gate). verify-desmenuzar.mjs covers both.
- **Planner mode ("sembrar a manos llenas", 0.0.41)**: the tree-view plant sheet's "Varios a la vez" toggle turns the input into a paste-friendly textarea — one line = one branch, Tab/2-space indent = child of the nearest shallower line above (`sow()` in `tree-view.ts`; Tab is intercepted to indent, never to walk focus). Header "+ Plantar aquí" keeps planting at ROOT level (null parent) — depth-0 lines inherit the sheet's target. Templates are CONTENT, not structure: the empty-meadow starter saplings plant ordinary renameable trees; `Settings.startersHidden` ("Prefiero empezar en blanco") hides them for good. A ≥6-branch sheet session earns ONE burst invitation toast on close (2-minutitos door; never repeats, skipped during live sessions). The config surface stays finite — no custom fields, no automations.
- **Canvas taps resolve at the SVG level to the NEAREST node center** (`onCanvasClick` in `tree-canvas.ts`): per-node `.hit` discs are cosmetic hover zones only and must never outgrow the slot spacing. Never reintroduce per-node click handlers — stacked transparent discs used to swallow neighbors' taps. The `plant-bud`/`note-mark` win on direct hit (`stopPropagation` + an explicit target guard).
- **Framing**: `fitTree()` frames the WHOLE tree once per tree id (zoom floor 0.5; when even that overflows, it keeps 0.5, biases to the 📍 and `fitsWhole=false` unlocks mouse-pan). Planting pans the newborn into view; status flips and renames must never move the camera (the fit effect is id-keyed on purpose).
- Wood color: bark→moss mix by depth; `origin:'branch'` limbs lean golden (`--status-branched`).
- **Flora species (0.0.64: SIXTEEN shapes, 3 cousins per accent)**: `flora.ts` maps the tree's accent → a COLOR FAMILY with three cousin silhouettes (classic first for continuity: petal5/daisy/bell/star; 0.0.61 second: poppy/lupine/anemone/trumpet; 0.0.64 third from the owner's reference sheet: **sunflower/clover/tulip/carnation/pinwheel/jasmine/spray/dahlia** — moss+clover, sage+spray, sky+jasmine, clay+sunflower, lavender+tulip, sand+dahlia, rose+carnation, pine+pinwheel); `flowerFor(accent, seedId?)` picks the cousin by `hash(treeId) % variants.length` — a whole tree blooms ONE way, same accent reads as the same species (hue dominates), same-accent trees may bloom differently. Growing 2→3 cousins changed the modulo, so existing trees re-picked their silhouette ONCE (deterministic, same family — owner consented 2026-07-11, "reparto parejo" over minimal churn). Rendered by `g[appFlower]` everywhere (canvas glyphs, branch blossoms, minis, trunk-base flowers, meadow flowers — meadow passes `node.treeId` so meadow blooms match their tree's variant). All shapes live inside the ~12px envelope; call-site scales are fixed. Flowers stay achieved-only at every site. Unknown shapes fall through to the `petal5` `@default`. Review battery: `tools/shoot-flora.mjs` (opportunistic cousins); for an exhaustive per-shape pass, temporarily force `flowerFor`'s return and revert.
- Weather is split in two layers: `SceneBackdrop` (BEHIND the scenery: clouds, lightning, sky mist banks, fog-seam, mountain dissolve) and `WeatherFront` (OVER the scenery, pointer-transparent: the rain itself and the fog's ground veil). Rain/fog must never end at the landscape's edge — that hard line is why the split exists. Both take the same `mood` input (latest check-in feeling); `?mood=` query override for demos.
- Tree view ground is **pinned to the meadow band** (both computed from the same `.canvas-wrap` box) — do not reintroduce independent anchors; that caused "floating tree" bugs three times.

## Dev workflow

```bash
npm ci && npm start        # Node ≥ 24.15 (26.x recommended)
npm run build              # prod build → dist/roadmap2u/browser
cd infra && npm ci && npm test && npx cdk synth   # backend: 16 vitest + template, zero AWS needed
```

- **Demo data**: `?seed=demo` on an EMPTY store loads a showcase forest with FIXED ids (`demo-guitar`, `demo-health` — has a yesterday-dated branch for the review flow, `demo-work`, `demo-seedling`). Routes like `/tree/demo-guitar?seed=demo` are stable. Never touches real data.
- **Overrides**: `?theme=organic|terminal`, `?mood=sunny|calm|foggy|heavy|stormy` (session-only).
- **Verification is interactive, via playwright-core + system Edge** (`channel: 'msedge'`, no browser download): `tools/shoot.mjs` (viewport-true screenshots), `tools/inspect-forest.mjs` (computed-layout probe), `tools/verify-circle.mjs`, `tools/verify-archive.mjs`, `tools/verify-forest-archive.mjs` (drive full flows). `tools/gen-icons.mjs` regenerates PWA icons from `public/icons/logo.svg`.
- **Frontend unit tests**: `npx ng test` (vitest, 50 tests across 6 specs — pure brains only: ranker, LWW, time, layout, finder, flora). Run alongside the playwright battery on every ship.
- **BATTERY LAW (0.0.77, learned the hard way)**: the local server MUST carry the SPA fallback (`npx http-server dist/roadmap2u/browser -p 8826 -s -P "http://localhost:8826?"`) or every deep `page.goto` dies; and a verify run FAILS if its **exit code is non-zero** OR the FULL output greps `OK=false|PAGEERROR|Error:` — never grep only the tail (a crashed probe ends with an error-object dump whose last lines match nothing; two releases shipped on a green-looking battery that had crashed scripts). **Run the battery via `node tools/run-battery.mjs`** (all scripts; or `run-battery.mjs undo perch` for a subset) — it encodes the whole law: server preflight on a deep route, per-script exit-code + full-output grep, 240s timeout, logs in `tools/battery-logs/`, exit 1 on any red. Gate the commit on its exit code, never chain `battery && commit`.
- **Harness (`tools/lib/harness.mjs`)**: `launchPage()`, `BASE`, `ok(label, cond, detail)` (prints `OK=` AND sets the exit code), `nodeCenter(page, nth)`, `signInAs(page, u, p)`, `skipRitual(page)`. NEW verify scripts must use it (verify-backup.mjs is the exemplar); existing scripts migrate whenever a change touches them — hand-copied preambles are how probes drift from the app (the 0.0.75 fold silently broke four).
- **Playwright strict-mode law**: give every NEW toggle/control its own semantic class (`.order-toggle`, `.repeats-toggle`, `.energy-toggle`) — reusing an existing class (`.flow-toggle`, `.note-toggle`) makes two elements match one locator and breaks the verify battery (bitten in 0.0.72 AND 0.0.75). Share styles in scss (`.note-toggle, .energy-toggle { … }`), never share the class.
- Known environment traps: **IndexedDB is dead in some sandboxed headless browsers** (app falls back to memory — persistence must be verified in a real browser); **`msedge --headless --window-size` screenshots lie about mobile layout** — use the playwright tools instead.

## Deploy (GitHub Pages)

- Push to `main` → `.github/workflows/deploy.yml` (build with `--base-href /RoadMap2U/` — **exact casing**, copy `index.html→404.html`, `.nojekyll`, upload, deploy with an in-run 90s retry).
- SW registration MUST stay relative (`'ngsw-worker.js'`); a leading slash breaks under the subpath. Manifest `start_url`/`scope` are `./`.
- **Version tag**: pre-launch semver in `core/version.ts` — `'0.0.N · <date> — <release name>'`, bumped on every notable deploy (rendered twice in Settings).
- **Pages flake playbook** (encountered repeatedly): `deploy-pages` fails with "Deployment failed, try again later" while status is operational. Reruns reuse the deployment id (= commit SHA) and keep failing — **push a fresh (empty) commit instead**. If no run appears after a push at all (dropped webhook), `workflow_dispatch` the workflow. Deep links returning HTTP 404 with the app body is expected Pages SPA behavior, not a bug.

## Deliberately NOT built (do not "fix" these)

- No event-log store, no merge-import, no per-record dirty flags — **the sync engine IS built** (`core/sync/sync.service.ts`, 0.0.53) and it deliberately needs none of those: outbound is a WATERMARK scan (`updatedAt > watermark`, incl. tombstones/archived) triggered by `onLocalWrite` (debounced) + boot; inbound walks the server cursor into LWW-guarded disk puts + `RecordsRepo.applyExternal`; re-pushing is idempotent by contract (STALE_REV + server winner), so every edge self-heals. Strictly OPT-IN via `account.link` (`connect()` = full push then pull; `disconnect()` never touches local data; mismatched account ⇒ sync off + banner). The `applyingRemote` flag stops pull→push echo; pulls re-broadcast per store for other tabs. Friends/visits UI for non-family arrives with the «amigos» phase (`docs/backend-contract.md` §rollout).
- Priority exists ONLY as «la luz» (owner override, 2026-07-09, of the former "no priority fields — executive-function tax" rule; 0.0.60): `TreeNode.priority?: 'sunlit' | 'shade' | null` (SCHEMA_VERSION 4, additive; absent ≡ the steady default «a su ritmo» — no record ever stores "normal"). What keeps it from becoming the tax the old rule feared, structurally: never required (absent forever-default), never numeric/counted/aged, never red, never auto-set by the system, and it NEVER sorts the forest or writes sibling `order` — the tablita's «Ver por luz» is a session-scoped LENS (re-sorts the list only; steps groups keep their 1./2./3.; tree shape stays history). In the ranker it BIASES, never tyrannizes: the sunlit bucket sits BELOW today's intentions and when-then twigs (the user's explicit now always outranks standing light); shade yields ambient resurfacing (momentum skips it, fresh buckets sort it last) but stays reachable via "Otra idea" and pickable as a today-intention — deliberate paths never check the shade. Shade ≠ `resting`: shade mutes ambient echo, resting pauses the branch. Canvas (0.0.62 — deliberately UNMISSABLE, owner's ADHD-first requirement "el cue visual debe ser más marcado"): sunlit = a REAL sun (bright r40 glow, core opacity ~0.95, slow 6s breathe — static under reduce-motion) + 8 defined rays (`.sun-rays`, 40s turn, `transform-box: fill-box`) + the glyph scaled ×1.35 (`glyphScale`) + a GOLDEN LABEL PILL (`.label-pill.sun`, solid fill, dark ink via `.title.in-pill` which drops the bg-stroke halo — the pill IS the contrast) + rayito + title ×1.12. Shade = a VISIBLE 4-leaf parasol arched over the node + a cool grey-green pill (`.label-pill.shade`) — ADDED elements, never dimming or shrinking the branch (shame-by-typography stays banned). Pill geometry comes straight from `LabelBlock` (pad 5/3 < the packer's 16px margin → pills can never collide; `pillFor` in tree-canvas.ts). Live statuses only (seed/growing); stale values on achieved/resting records persist silently and stay inert everywhere. Stripped visits null it like `trigger` (attention allocation is intimate); guardians see and set it. Minis stay clean; the MEADOW sign shows a small `.sign-sun` ☀️ when a tree holds sunlit branches (forest-level triage). Verify class names that must survive: `.light-halo`, `.rayito`, `.sun-badge`, `.label-pill`, `.sun-rays`, `.sign-sun`.
- No push notifications, streaks, or usage nudges. The former "stale-branch nudge" deferral was resolved in v32 as the **pull-based** `/trail` "Ramas dormidas" section (30 days, dateless seed/growing LEAVES only, `resting` exempt, no day counters, renders only when non-empty) — do not convert it into a push notification or a forest-side badge.

## Docs to keep in sync when features change

`docs/manual-usuario.md` (source of the Word handout — regenerate with `pandoc docs/manual-usuario.md -o Manual_RoadMap2U.docx --toc --toc-depth=2`) and the in-app guide dictionaries (`guide` section in `es.ts`/`en.ts`).
