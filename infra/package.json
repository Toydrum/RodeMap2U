{
  "name": "rodemap-infra",
  "version": "0.0.1",
  "private": true,
  "description": "AWS backend for RodeMap2U — CDK stack + Lambda router implementing src/app/core/api/contracts.ts (see docs/backend-contract.md and docs/aws-connect.md).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "synth": "cdk synth --quiet",
    "deploy": "cdk deploy",
    "diff": "cdk diff"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.700.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^24.0.0",
    "aws-cdk": "^2.170.0",
    "aws-cdk-lib": "^2.170.0",
    "aws-sdk-client-mock": "^4.1.0",
    "constructs": "^10.4.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0",
    "typescript": "~5.7.0",
    "vitest": "^4.0.0"
  }
}
