# RodeMap2U

Read `AGENTS.md` at the repo root — it is the single source of onboarding truth for this project (product rules, architecture semantics, dev workflow, deploy playbook). Everything there applies to Claude Code sessions.
