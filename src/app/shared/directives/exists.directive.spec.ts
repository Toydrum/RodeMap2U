import { ExistsDirective } from './exists.directive';

describe('ExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistsDirective();
    expect(directive).toBeTruthy();
  });
});
